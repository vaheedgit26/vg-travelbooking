# Terraform state (stored in GCS, not locally)
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform/
.terraform.lock.hcl

# Service account key — NEVER commit this
keys.json
*.json

# OS files
.DS_Store
