apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}   # redis-svc
  namespace: {{ .Values.namespace }} # travelbooking-dev
spec:
  selector:
    app: redis
  ports:
  - port: {{ .Values.service.port }}       # 6379
    targetPort: {{ .Values.service.targetPort }} # 6379
