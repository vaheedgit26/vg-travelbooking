apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.notificationService.servicename }}
  namespace: {{ .Values.namespace }}
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
  - name: http
    port: 3005
    targetPort: 3005
