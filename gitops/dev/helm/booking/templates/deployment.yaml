apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.booking.deployment.name }}
  namespace: {{ .Values.booking.deployment.namespace }}
  labels:
    app: booking-service
spec:
  replicas: {{ .Values.booking.deployment.replicas }}
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
    spec:
      containers:
      - name: booking-service
        image: {{ .Values.bookingService.image }}
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 3003
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        envFrom:
        - configMapRef:
            name: {{ .Values.booking.configmap.name }}   # travelbooking-booking-configg
        - secretRef:
            name: {{ .Values.booking.secret.name }}   # travelbooking-dev-postgre-jwt-credentials
