apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: search-service
spec:
  type: ClusterIP
  selector:
    app: search-service
  ports:
  - name: http
    port: {{ .Values.service.port }} # 3002
    targetPort: {{ .Values.service.targetPort }} # 3002
